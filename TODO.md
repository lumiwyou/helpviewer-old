### Install helper
- CLI and WebUI
- Crawl the ``microsoft_root_url=https://services.mtps.microsoft.com/serviceapi/products`` directory and list all the links to products, then let user install them.

### Issues
- Issue: the batch page system thing is shit and broke it

### General
- Use static() instead of file?get= for file retrieval
